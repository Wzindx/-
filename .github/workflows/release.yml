name: Release Android APK

on:
  workflow_run:
    workflows: ["Build Android APK"]
    types:
      - completed
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to publish to, e.g. v2.1. Leave empty to use app versionName."
        required: false
        type: string
        default: ""

concurrency:
  group: android-release-${{ github.event.workflow_run.head_branch || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout release source
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha || 'main' }}
          fetch-depth: 0

      - name: Resolve release variables
        shell: bash
        run: |
          set -euo pipefail

          VERSION_NAME="$(grep -oP 'versionName\s*=\s*"\K[^"]+' app/build.gradle.kts | head -n 1)"
          TAG_NAME="${{ github.event.inputs.tag_name || '' }}"
          if [ -z "$TAG_NAME" ]; then
            TAG_NAME="v$VERSION_NAME"
          fi

          RELEASE_ENABLED="true"
          RELEASE_SHA="$(git rev-parse HEAD)"

          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            RELEASE_SHA="${{ github.event.workflow_run.head_sha }}"
            git fetch origin main --depth=1
            LATEST_MAIN_SHA="$(git rev-parse origin/main)"
            if [ "$RELEASE_SHA" != "$LATEST_MAIN_SHA" ]; then
              echo "Skip stale release: workflow_run SHA $RELEASE_SHA is not latest main $LATEST_MAIN_SHA."
              RELEASE_ENABLED="false"
            fi
          fi

          echo "VERSION_NAME=$VERSION_NAME" >> "$GITHUB_ENV"
          echo "TAG_NAME=$TAG_NAME" >> "$GITHUB_ENV"
          echo "RELEASE_SHA=$RELEASE_SHA" >> "$GITHUB_ENV"
          echo "RELEASE_ENABLED=$RELEASE_ENABLED" >> "$GITHUB_ENV"

      - name: Set up JDK 17
        if: env.RELEASE_ENABLED == 'true'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        if: env.RELEASE_ENABLED == 'true'
        uses: android-actions/setup-android@v4

      - name: Configure Android SDK path
        if: env.RELEASE_ENABLED == 'true'
        shell: bash
        run: |
          set -euo pipefail
          SDK_DIR="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-/usr/local/lib/android/sdk}}"

          if [ ! -d "$SDK_DIR" ]; then
            echo "Android SDK directory does not exist: $SDK_DIR"
            exit 1
          fi

          echo "ANDROID_HOME=$SDK_DIR" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDK_DIR" >> "$GITHUB_ENV"
          echo "sdk.dir=$SDK_DIR" > local.properties

          echo "Configured Android SDK:"
          cat local.properties

      - name: Prepare release signing
        if: env.RELEASE_ENABLED == 'true'
        shell: bash
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          if [ -z "${ANDROID_KEYSTORE_BASE64:-}" ]; then
            echo "ANDROID_KEYSTORE_BASE64 is not configured; release APK will use default signing behavior."
            exit 0
          fi

          mkdir -p "$RUNNER_TEMP/android-signing"
          KEYSTORE_PATH="$RUNNER_TEMP/android-signing/release.jks"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$KEYSTORE_PATH"
          chmod 600 "$KEYSTORE_PATH"

          echo "ANDROID_KEYSTORE_PATH=$KEYSTORE_PATH" >> "$GITHUB_ENV"
          echo "ANDROID_KEYSTORE_PASSWORD=$ANDROID_KEYSTORE_PASSWORD" >> "$GITHUB_ENV"
          echo "ANDROID_KEY_ALIAS=$ANDROID_KEY_ALIAS" >> "$GITHUB_ENV"
          echo "ANDROID_KEY_PASSWORD=$ANDROID_KEY_PASSWORD" >> "$GITHUB_ENV"

      - name: Build Release APK
        if: env.RELEASE_ENABLED == 'true'
        shell: bash
        run: |
          set -euo pipefail

          if [ ! -f ./gradlew ]; then
            echo "Missing Gradle Wrapper script: ./gradlew"
            exit 1
          fi

          if [ ! -f ./gradle/wrapper/gradle-wrapper.jar ]; then
            echo "Missing Gradle Wrapper jar: ./gradle/wrapper/gradle-wrapper.jar"
            exit 1
          fi

          chmod +x ./gradlew
          ./gradlew --version
          test -f local.properties
          cat local.properties
          ./gradlew :app:assembleRelease --no-daemon --stacktrace

          mkdir -p dist
          RELEASE_APK="$(find app/build/outputs/apk/release -name '*release*.apk' -print -quit 2>/dev/null || true)"
          test -n "$RELEASE_APK"
          cp "$RELEASE_APK" dist/app-release.apk
          ls -lah dist/app-release.apk

      - name: Publish APK to GitHub Release
        if: env.RELEASE_ENABLED == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          BODY_FILE="$RUNNER_TEMP/release-body.md"
          cat > "$BODY_FILE" <<'BODY'
          本版本是 ImageForge v2.1 精修版，重点完善图片生成后的结果预览、保存、打开、分享、历史记录回放、后台状态、错误诊断、自定义保存路径与自动构建发布链路。

          ## v2.1 优化重点

          - 正常生成图片后继续停留在当前“结果预览”页面，不再自动跳转到“图片记录”页；图片记录仅作为归档入口。
          - 结果预览页的保存、分享全部加入异常兜底，保存失败或分享失败只显示明确提示，不再导致应用闪退。
          - 网络请求对 unexpected end of stream / EOFException / 空响应断流增加友好诊断，并为请求加入 Connection: close，降低 HTTP/1.1 连接复用导致的异常。
          - 修复自定义保存目录使用 SAF tree URI 时保存失败的问题，解决 Invalid URI: content://.../tree/... 导致图片保存、分享和历史预览异常。
          - 错误详情复制升级为完整任务信息与完整异常堆栈，图片记录详情页可直接复制完整报错，便于定位 Base URL、模型、接口模式与网络问题。
          - 生成按钮文案改为“生成图像”，有任务进行中时复用为“取消生成图像”，避免重复按钮和语义混乱。
          - 图片记录页补齐“打开 / 保存 / 分享”能力，与结果预览页共用同一套安全图片操作逻辑。
          - 分享和打开图片前会先校验 content URI 是否可读，避免系统 Intent 权限异常导致闪退。
          - 生成接口返回成功但图片保存失败时，不再标记为“处理成功”；会改为“处理失败”，并展示“图片生成成功，但保存图片文件失败”的可复制详情。
          - 历史记录详情改为“图片预览”，正常生成结果优先展示图片；无可读图片时只保留描述和失败/缺失说明，不展示无效图片按钮。
          - running、failed、success 状态明确区分，处理中不会误显示为失败。
          - 删除入口收敛为长按选择删除，避免误触和界面冗余。
          - 错误提示优先保留 HTTP 状态码、响应体和完整异常，便于定位 401、403、429、500、503 等问题。
          - 图片保存路径支持系统目录选择和授权，设置页尽量展示 /storage/emulated/0/... 形式的直观路径。
          - 补强通知权限与保存目录权限引导，完成生成后可通过通知进入结果。
          - 完整验证 Gradle Wrapper、Android Lint、Debug APK、Release APK 与自动发布链路。

          ## 主要能力

          - 文本生成图片。
          - 参考图 / 图生图编辑。
          - OpenAI 兼容图像接口配置。
          - 自定义 Base URL、API Key、接口模式、模型、尺寸、质量、格式和生成数量。
          - 后台生成任务、完成通知、历史记录查看、图片保存与分享。
          - 移动端优先的参数选择和引导体验。

          ## 下载

          下载下方 `app-release.apk` 安装即可。

          - 包名：`com.yang.emperor`
          - 版本：`__VERSION_NAME__`
          - 文件：`app-release.apk`

          ## 验证状态

          - Android Lint：通过 CI 执行。
          - Debug APK：通过 CI 构建。
          - Release APK：通过 CI 构建并自动上传到 GitHub Release。
          BODY

          sed -i "s/__TAG_NAME__/$TAG_NAME/g; s/__VERSION_NAME__/$VERSION_NAME/g" "$BODY_FILE"

          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release edit "$TAG_NAME" \
              --target "$RELEASE_SHA" \
              --title "ImageForge $TAG_NAME" \
              --notes-file "$BODY_FILE" \
              --latest
          else
            gh release create "$TAG_NAME" \
              --target "$RELEASE_SHA" \
              --title "ImageForge $TAG_NAME" \
              --notes-file "$BODY_FILE" \
              --latest
          fi

          gh release delete-asset "$TAG_NAME" app-release.apk -y || true
          gh release delete-asset "$TAG_NAME" app-debug.apk -y || true
          gh release upload "$TAG_NAME" dist/app-release.apk --clobber